Empty for now. Phase 2 seed scripts should populate data shaped like the mock arrays
already in `apps/builder/src/services/*.ts` (e.g. the sample projects in
`projectService.ts`) so local Postgres-backed dev matches today's mock-data dev
experience.
