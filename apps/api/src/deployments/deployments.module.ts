import { Module } from '@nestjs/common'

// Scaffold only — Phase 2. No deployment target (static export? container push?
// a managed platform?) has been chosen yet — no endpoint here until it is.
@Module({})
export class DeploymentsModule {}
