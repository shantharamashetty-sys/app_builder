import { Module } from '@nestjs/common'

// Scaffold only — Phase 2. Preview semantics depend entirely on how the (unbuilt)
// generation pipeline populates generated/previews/ — no endpoint here yet.
@Module({})
export class PreviewModule {}
