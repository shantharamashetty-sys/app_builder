// Parallels ThemeOption in apps/builder's ProjectWizard.ts model.

export interface ThemeSchema {
  id: string
  primaryColor: string
  secondaryColor: string
  accentColor: string
  radii?: string
  fontFamily?: string
}
